{{>header}}

<main role="main">

    <section class="jumbotron text-center">
        <div class="container">
            <h1 class="jumbotron-heading display-4">Twaddle</h1>
            <p class="lead text-muted">Like Twitter, but worse!</p>
        </div>
    </section>

    <div class="container">
        {{#users}}
            <div class="row justify-content-center">
                {{>user}}
            </div>
        {{/users}}
    </div>

</main>

{{>footer}}